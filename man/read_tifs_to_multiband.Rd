% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_tifs_to_multiband.R, R/utils.R
\name{read_tifs_to_multiband}
\alias{read_tifs_to_multiband}
\title{Read TIF Files into a Multiband Raster Object}
\usage{
read_tifs_to_multiband(tif_directory)

read_tifs_to_multiband(tif_directory)
}
\arguments{
\item{tif_directory}{Character, the path to the directory
containing \code{.tif} files.}
}
\value{
A multiband raster object (\code{SpatRaster}) where each band
corresponds to a \code{.tif} file in the directory, named after the
corresponding file.

A multiband raster object (\code{SpatRaster}) where each band
corresponds to a \code{.tif} file in the directory, named after the
corresponding file.
}
\description{
Reads all \code{.tif} files from a specified directory and combines
them into a single multiband raster object. Each band in the
resulting raster corresponds to one of the TIF files, and the
band names are derived from the TIF file names (without the
\code{.tif} extension).

Reads all \code{.tif} files from a specified directory and combines
them into a single multiband raster object. Each band in the
resulting raster corresponds to one of the TIF files, and the
band names are derived from the TIF file names (without the
\code{.tif} extension).
}
\examples{
# Example usage of the function
tif_directory <- "path/to/tif/files"
multiband_raster <- read_tifs_to_multiband(tif_directory)
print(multiband_raster) # Display the multiband raster object
plot(multiband_raster) # Plot the raster bands
# Example usage of the function
tif_directory <- "path/to/tif/files"
multiband_raster <- read_tifs_to_multiband(tif_directory)
print(multiband_raster) # Display the multiband raster object
plot(multiband_raster) # Plot the raster bands
}
