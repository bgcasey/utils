% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel_extract_directory.R, R/utils.R
\name{parallel_extract_directory}
\alias{parallel_extract_directory}
\title{Parallel Extraction of Raster Data Stored in a Directory}
\usage{
parallel_extract_directory(
  locations,
  tif_directory,
  bands,
  extract_fun = extract_by_year,
  extract_args = list(fun = "mean"),
  extra_exports = NULL
)

parallel_extract_directory(
  locations,
  tif_directory,
  bands,
  extract_fun = extract_by_year,
  extract_args = list(fun = "mean"),
  extra_exports = NULL
)
}
\arguments{
\item{locations}{sf or Spatial object. Geographic features for which
raster values are extracted.}

\item{tif_directory}{Character. The directory path containing the
TIF files.}

\item{bands}{Character vector. The subset of raster bands to extract
from each TIF file.}

\item{extract_fun}{Function. The function used to perform the
extraction. Defaults to \code{extract_by_year} (replace as needed).}

\item{extract_args}{List. Additional arguments passed on to
\code{extract_fun}. Defaults to \code{list(fun = "mean")}.}

\item{extra_exports}{Character vector. Names of additional objects
or functions to export to the cluster environment, if needed.}
}
\value{
A list of extraction results, one element per feature in
\code{locations}.

A list of extraction results, one element per feature in
\code{locations}.
}
\description{
Executes a parallelized approach to extract values from multi-band
raster files using a custom extraction function for each spatial
feature in \code{locations}.

Executes a parallelized approach to extract values from multi-band
raster files using a custom extraction function for each spatial
feature in \code{locations}.
}
\examples{
# Example usage of the function
# Suppose you have a set of locations (e.g., sf polygons or points)
# buffers <- sf::read_sf("path/to/shapefile.shp")

# result <- parallel_extract_directory(
#   locations     = list of location buffers,
#   tif_directory = "path/to/tifs",
#   bands         = c("SR_B1", "NDVI"),
#   extract_fun   = extract_by_year,
#   extract_args  = list(fun = "mean")
# )

# print(result)

# Example usage of the function
# Suppose you have a set of locations (e.g., sf polygons or points)
# buffers <- sf::read_sf("path/to/shapefile.shp")

# result <- parallel_extract_directory(
#   locations     = list of location buffers,
#   tif_directory = "path/to/tifs",
#   bands         = c("SR_B1", "NDVI"),
#   extract_fun   = extract_by_year,
#   extract_args  = list(fun = "mean")
# )

# print(result)

}
