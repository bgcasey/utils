% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_tifs_to_list.R, R/utils.R
\name{read_tifs_to_list}
\alias{read_tifs_to_list}
\title{Read TIF Files into a Named List}
\usage{
read_tifs_to_list(tif_directory, tif_list = list())

read_tifs_to_list(tif_directory, tif_list = list())
}
\arguments{
\item{tif_directory}{Character, the path to the directory
containing \code{.tif} files.}

\item{tif_list}{List, an optional existing list to append the
raster objects to. Defaults to an empty list.}
}
\value{
A list where each element is a raster object, named
after the corresponding TIF file.

A list where each element is a raster object, named
after the corresponding TIF file.
}
\description{
Reads all \code{.tif} files from a specified directory and stores
them as raster objects in a named list. The names of the list
elements are derived from the TIF file names (without the \code{.tif}
extension).

Reads all \code{.tif} files from a specified directory and stores
them as raster objects in a named list. The names of the list
elements are derived from the TIF file names (without the \code{.tif}
extension).
}
\examples{
# Example usage of the function
tif_directory <- "path/to/tif/files"
tif_list <- read_tifs_to_list(tif_directory)
print(names(tif_list)) # Display names of list elements
# Example usage of the function
tif_directory <- "path/to/tif/files"
tif_list <- read_tifs_to_list(tif_directory)
print(names(tif_list)) # Display names of list elements
}
