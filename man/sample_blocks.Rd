% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_blocks.R
\name{sample_blocks}
\alias{sample_blocks}
\title{Sample a fraction of rows within blocks}
\usage{
sample_blocks(data, frac = 0.05, block_column)
}
\arguments{
\item{data}{A data frame containing the data to sample from.}

\item{frac}{Numeric. Fraction of rows to sample from each
block (default = 0.05).}

\item{block_column}{Character. The name of the column in
\code{data} that defines blocks.}
}
\value{
A data frame containing the sampled rows, with
approximately \code{frac} rows per block.
}
\description{
This function takes a data frame and samples a specified
fraction of rows within each block, defined by a given
column. Sampling is done without replacement, so each row
can appear at most once in the output.
}
\examples{
set.seed(123)
df <- data.frame(
    block_id = rep(letters[1:4], each = 1000),
    value = rnorm(4000)
)
df_sub <- sample_blocks(
    df,
    frac = 0.05, block_column = "block_id"
)
dplyr::count(df_sub, block_id)
}
